# Remove Metadata

A collection of patches which cummulatively remove `Metadata`, `scale-info`
from `polkadot-sdk`. The patches are increasingly more likely to require
updates when the version of the `polkadot-sdk` derived from is updated as they
go on. Long-term support for patches 4 and 5 are not guaranteed.
