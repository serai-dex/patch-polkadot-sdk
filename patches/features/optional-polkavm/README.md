# Optional PolkaVM

- `substrate-wasm-builder`: Patches `substrate-wasm-builder` with the
  `polkavm-linker` feature.

- `sc-executor`: Patches `substrate/client/executor` with the `polkavm`
  feature.
